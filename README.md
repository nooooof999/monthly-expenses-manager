# 📊 برنامج إدارة المصروفات - Excel بدون ماكرو

## نظرة عامة
ملف Excel ذكي يستخدم الصيغ المتقدمة فقط (بدون برمجة VBA) لتحليل بيانات WhatsApp تلقائياً.

## الهيكل:

### 1. ورقة "إدخال_WhatsApp"
- عمود A: لصق النص الكامل من WhatsApp
- لا تعديل - فقط نسخ ولصق

### 2. ورقة "معالجة_البيانات"
الأعمدة:
- A: رقم السطر (مرجع)
- B: النص الأصلي
- C: التاريخ المستخرج (صيغة)
- D: المبلغ المستخرج (صيغة)
- E: الوصف المستخرج (صيغة)
- F: الفئة التلقائية (صيغة)
- G: الشهر المالي (27-26) (صيغة)
- H: السنة المالية (صيغة)

### 3. ورقة "قاعدة_البيانات_النهائية"
بيانات منظفة ومرتبة جاهزة للتحليل

### 4. ورقة "لوحة_المعلومات"
- إجمالي المصروفات للدورة الحالية
- متوسط اليومي
- توزيع الفئات مع رسوم بيانية
- مقارنة مع الدورات السابقة

### 5. ورقات منفصلة لكل دورة مالية:
- "دورة_2024_11" (نوفمبر 27 إلى ديسمبر 26)
- "دورة_2024_12" (ديسمبر 27 إلى يناير 26)
- تُنشأ تلقائياً حسب البيانات

## الصيغ الرئيسية المستخدمة:

### استخراج التاريخ:
```excel
=IF(OR(ISNUMBER(SEARCH("يناير",B2)),ISNUMBER(SEARCH("فبراير",B2)),ISNUMBER(SEARCH("مارس",B2)),ISNUMBER(SEARCH("أبريل",B2)),ISNUMBER(SEARCH("مايو",B2)),ISNUMBER(SEARCH("يونيو",B2)),ISNUMBER(SEARCH("يوليو",B2)),ISNUMBER(SEARCH("أغسطس",B2)),ISNUMBER(SEARCH("سبتمبر",B2)),ISNUMBER(SEARCH("أكتوبر",B2)),ISNUMBER(SEARCH("نوفمبر",B2)),ISNUMBER(SEARCH("ديسمبر",B2))),B2,"")
```

### استخراج المبلغ:
```excel
=IF(ISNUMBER(VALUE(LEFT(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(B2,"٠","0"),"١","1"),"٢","2"),"٣","3"),"٤","4"),"٥","5"),"٦","6"),"٧","7"),"٨","8"),"٩","9"),FIND(" ",SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(B2,"٠","0"),"١","1"),"٢","2"),"٣","3"),"٤","4"),"٥","5"),"٦","6"),"٧","7"),"٨","8"),"٩","9"))-1))),VALUE(LEFT(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(B2,"٠","0"),"١","1"),"٢","2"),"٣","3"),"٤","4"),"٥","5"),"٦","6"),"٧","7"),"٨","8"),"٩","9"),FIND(" ",SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(B2,"٠","0"),"١","1"),"٢","2"),"٣","3"),"٤","4"),"٥","5"),"٦","6"),"٧","7"),"٨","8"),"٩","9"))-1)),""
```

### حساب الشهر المالي (27-26):
```excel
=IF(DAY(C2)<27,TEXT(C2,"yyyy-mm"),TEXT(DATE(YEAR(C2),MONTH(C2)+1,1),"yyyy-mm"))
```

### التصنيف التلقائي:
```excel
=IF(OR(ISNUMBER(SEARCH("مطع��",E2)),ISNUMBER(SEARCH("بقالة",E2)),ISNUMBER(SEARCH("كافي",E2))),"طعام وشراب",
IF(OR(ISNUMBER(SEARCH("بنزين",E2)),ISNUMBER(SEARCH("توصيلة",E2))),"مواصلات",
IF(OR(ISNUMBER(SEARCH("حلاق",E2)),ISNUMBER(SEARCH("ملابس",E2))),"شخصي",
IF(OR(ISNUMBER(SEARCH("مدرسة",E2)),ISNUMBER(SEARCH("علاج",E2))),"صحة وتعليم",
IF(OR(ISNUMBER(SEARCH("امازون",E2)),ISNUMBER(SEARCH("تريندول",E2))),"منزل ومستلزمات","أخرى")))))
```

## خطوات الاستخدام:

1. افتح ورقة "إدخال_WhatsApp"
2. انسخ بيانات WhatsApp كاملة
3. الصقها في عمود A
4. انتقل إلى "لوحة_المعلومات" لرؤية التحليلات
5. تُحدّث البيانات تلقائياً

## المميزات:
✅ لا يحتاج تفعيل ماكرو
✅ يعمل فوراً بدون إعداد
✅ صيغ ذكية تفهم العربية
✅ تحديث تلقائي للتحليلات
✅ أرشفة تلقائية للدورات
✅ رسوم بيانية تفاعلية

## الدورة المالية:
- تبدأ في 27 من كل شهر
- تنتهي في 26 من الشهر التالي
- كل دورة في ورقة منفصلة للأرشفة

تم الإنشاء بواسطة: @nooooof999
tاريخ: 2024-11-12